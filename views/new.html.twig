{% extends "layout.html.twig" %}

{% block main %}
    <form action="{{ path('game_create') }}" method="POST">
        <table>
            <tr><td>
            <label for="urls">URLs à checker (une par ligne) :</label>
            <textarea rows="6" cols="50" name="urls" placeholder="http://www.google.fr"></textarea>
            </td></tr>
            <tr><td>
            <input type="submit" value="Créer le jeu" />
            </td></tr>
        </table>
    </form>
{% endblock %}